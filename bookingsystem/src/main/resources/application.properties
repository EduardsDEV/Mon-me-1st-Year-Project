!Taken from https://spring.io/guides/gs/accessing-data-mysql/

#spring.jpa.hibernate.ddl-auto can be: none, update, create, create-drop
#Refer to the Hibernate documentation for details.

#none This is the default for MySQL, no change to the database structure.
#update Hibernate changes the database according to the given @Entity structures.
#create Creates the database every time, but don’t drop it when close.
#create-drop Creates the database then drops it when the SessionFactory closes.
spring.jpa.hibernate.ddl-auto=none
spring.datasource.url=jdbc:mysql://localhost:3306/moname
spring.datasource.username=root
spring.datasource.password=
#spring.jackson.serialization.indent_output=true

#turn off the dates as timestamp jackson feature
#spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS = false